import styled from 'styled-components';

export const OurServiceCard = styled.div`
  p {
    margin-top: 24px;
    font-size: 24px;
  }
`;

export const LogoContainer = styled.div`
  width: clamp(133px, 12.5vw, 240px) ;
  height: clamp(133px, 12.5vw, 240px) ;
  
  background-color: #22202F;
  border-radius: 24px;
  padding: 30px;
`;

export const Image = styled.img`
  width: 90%;
`;
